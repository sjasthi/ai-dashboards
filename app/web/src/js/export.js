/**
 * export.js — handles report export actions.
 *
 * Currently a placeholder. Replace the alert() calls with real
 * implementations when ready:
 *
 *   PDF   → jsPDF (https://github.com/parallax/jsPDF)
 *   HTML  → generate a Blob and trigger a download link
 *   Email → POST to your backend / email API
 */
export function handleExport(reportType, format) {
  // TODO: implement real export
  alert(`Export "${reportType}" as ${format.toUpperCase()} — not yet implemented.`);
}
